#use:
#  dataProvider: hibernate
spring:
  shell:
    interactive:
      enabled: true
  datasource:

    url: jdbc:postgresql://localhost:5432/Library
    username: postgres
    password: westa852
    initialization-mode: always
  sql:
    init:
      mode: always
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: validate



    properties:
      hibernate:
        format_sql: true